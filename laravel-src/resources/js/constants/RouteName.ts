// 画面リンクの route名 定数
enum RouteName {
    // Google認証へのルート名
    GOOGLE_AUTH = 'google.index',
    // ホーム
    HOME = 'dashboard',
    // ダッシュボードへ
    DASHBOARD = 'dashboard',
    // ログアウト
    LOGOUT = 'logout',
    // 選手一覧 
    PLAYERS = 'player.search',
}

/* export */
export default RouteName;
